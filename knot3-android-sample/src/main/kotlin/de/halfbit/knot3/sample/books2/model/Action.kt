package de.halfbit.knot3.sample.books2.model

// Some delegates can serve other delegates by performing common actions like
// show error or similar.
// Such common actions should be added as inner classes to the Action interface.
// Actions, which are private to the delegates should be declared in the delegates.

interface Action
